# Variabile pentru docker-compose. Copiaza in .env si modifica.
# .env e gitignored - NU il committa.

# Postgres credentials
DB_USER=newsportal
DB_PASSWORD=newsportal_dev_change_me
DB_NAME=newsportal

# JWT signing key (genereaza cu: openssl rand -base64 48)
JWT_KEY=DevKeyAtLeast32BytesLongPleaseChangeMeForRealProd!!!
